import type { ReactElement } from 'react';

/**
 * Storefront shell.
 *
 * Routing, layout and screens land in Phase 5; Phase 0 only proves the app builds and runs.
 */
export function App(): ReactElement {
  return (
    <main>
      <h1>SilkGrain</h1>
      <p>Storefront — Phase 0 scaffold.</p>
    </main>
  );
}
