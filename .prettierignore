node_modules/
dist/
build/
coverage/
storybook-static/
playwright-report/
.turbo/
.services/
pnpm-lock.yaml

# Design handoff is read-only source material — never reformat it.
silkgrain-design-prompt/

# drizzle-kit writes these and rewrites them on every `db:generate`. Reformatting a generated
# journal only guarantees the next generate shows up as a diff against our own formatting.
apps/api/drizzle/meta/
